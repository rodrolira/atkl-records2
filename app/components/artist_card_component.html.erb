<!-- app/components/artist_card_component.html.erb -->
<div class="artist-card">
	<h3><%= @artist.name %></h3>
	<%= render SocialIconsComponent.new(icons: @artist.social_icons) %>
	<!-- Otros detalles del artista según tu diseño -->
</div>
