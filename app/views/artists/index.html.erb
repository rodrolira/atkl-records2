<!-- app/views/artists/index.html.erb -->
<div class="container mx-auto mt-56">
  <h2 class="text-2xl font-bold mb-4 text-white text-center">Artists</h2>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <% @artists.each do |artist| %>
      <%= render 'shared/artist_card', artist: artist %>
    <% end %>
  </div>
</div>
