<!-- app/views/discography/index.html.erb -->
<div class="container mx-auto mt-8">
	<h2 class="text-2xl font-bold mb-4">Discografía</h2>
	<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
		<!-- Aquí puedes iterar sobre tus álbumes o lanzamientos y mostrar la información -->
		<% @albums.each do |album| %>
			<div class="bg-white rounded-lg shadow p-4">
				<h3 class="text-lg font-bold mb-2"><%= album.title %></h3>
				<!-- Agrega más detalles según tu modelo de datos -->
			</div>
		<% end %>
	</div>
</div>
